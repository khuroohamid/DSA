#include <stdio.h>
#include <stdlib.h>

int main () {

    int myArray[100]; //Array Declaration
    int a[4] = {34,60,99,1}; //initializing while declaring
    float c[] = {-4,6,81,-60}; //dimension optional cause we initialized the array at the time of declaration.

    // A string in C
    char message[] = {'e', 'x', 'a', 'm', 'p', 'l', 'e', '\0'};
    char sentence[] = "Hello, this is a String in C.\n";

    //Multidimensional Arrays::
    int myMultiDimArr[exp1] [exp2] [expn];

    // p[] and &p[] are same

    return 0;
} 